# Replace the double space from problem 3 with single spaces.
s="Harry is good  boy"
print(s.replace("  "," "))
print(s) # strings are immutable which means that cannot change them by running function on them
